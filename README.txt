
argv_expand provides functions, wildArgvs(args[]) and wildArgv(arg), that can be used to expand pathname args for windows.  
These expand simple * or ? wildcards only in the basename of a pathname.  
There are a couple of example uses in programs cpd_main.d and rmd_main.d, in file_parallel/examples/
